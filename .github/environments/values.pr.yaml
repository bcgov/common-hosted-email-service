---
features:
  basicAuth: true
  oidcAuth: true
  defaultBucket: false

redis:
  image:
    registry: docker.io
    tag: latest
  replica:
    persistence:
      enabled: false
  sentinel:
    image:
      registry: docker.io
      tag: latest
    persistence:
      enabled: false
